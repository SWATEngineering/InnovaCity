apiVersion: 1

providers:
  - name: "Dashboards"
    type: file
    updateIntervalSeconds: 5
    allowUiUpdates: true
    options:
      path: /etc/grafana/dashboards_config
      foldersFromFilesStructure: true